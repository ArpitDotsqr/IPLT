import React from 'react'
import { getRole } from '../utils'
import AdminLayout from '../Layout/AdminLayout'
import AdminLogin from '../admin/AdminLogin'

const Main = () => {
    return (
        <>
            {/* {getRole() === 'admin' ? <AdminLayout /> : <AdminLogin />} */}
        </>
    )
}

export default Main